name: igraph &ndash; the network analysis package
future: false
pygments: true
include: [ .htaccess, .htpasswd, _static, _sources, _images ]
exclude: [ Makefile ]
markdown: redcarpet
redcarpet:
  extensions: [ "autolink" ]
