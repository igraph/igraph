
XML_INC_DIR=${LIB_XML}/include
XML_LIB_DIR=${LIB_XML}/lib

ifndef LIB_ICONV
 LIB_ICONV=.
endif


PKG_CPPFLAGS= -DUSING_R -DHAVE_FMEMOPEN=0 -DHAVE_OPEN_MEMSTREAM=0 -DWin32 -DHAVE_LIBXML -Wall -I ${XML_INC_DIR} -I ${LIB_ICONV}
PKG_LIBS= -L ${XML_LIB_DIR} -lxml2 


